# Autonomous SLAM E2E Acceptance Tests

M3-A4 acceptance requires scenario build pass, real adapter contract pass, SLAM backend acceptance pass, pre-live runner pass, no forward/backward command, stop command seen, active scan command seen when map quality is poor, valid map backend contract, valid pre-live report, hardware write disabled, production interface disabled, and no live motion.
